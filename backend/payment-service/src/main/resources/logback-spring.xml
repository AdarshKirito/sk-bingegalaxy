<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="unknown"/>

    <!--
        PCI-DSS log scrubbing: override the default "msg"/"m" conversion words so ALL
        pattern-based appenders automatically mask card PANs before writing.
        LogstashEncoder (kubernetes profile) uses MaskingMessageJsonProvider below.
    -->
    <conversionRule conversionWord="msg"
        converterClass="com.skbingegalaxy.common.logging.CardNumberMaskingConverter"/>
    <conversionRule conversionWord="m"
        converterClass="com.skbingegalaxy.common.logging.CardNumberMaskingConverter"/>

    <!-- ── Default (dev) profile: human-readable console ── -->
    <springProfile name="!kubernetes">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ── Kubernetes profile: structured JSON for Promtail / Loki ── -->
    <springProfile name="kubernetes">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <!-- PCI: replace default message provider with card-masking version -->
                <message class="com.skbingegalaxy.common.logging.MaskingMessageJsonProvider"/>
                <includeMdcKeyName>traceId</includeMdcKeyName>
                <includeMdcKeyName>spanId</includeMdcKeyName>
                <includeMdcKeyName>userId</includeMdcKeyName>
                <includeMdcKeyName>bingeId</includeMdcKeyName>
                <includeMdcKeyName>sentryTraceId</includeMdcKeyName>
                <customFields>{"service":"${APP_NAME}"}</customFields>
                <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <version>[ignore]</version>
                </fieldNames>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="JSON_CONSOLE"/>
        </root>
    </springProfile>
</configuration>
